### vsCode ###
.vscode

### macOS ###
# General
.DS_Store
.AppleDouble
.LSOverride
# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

**/node_modules
**/dist
**/build
**/.DS_Store
**/.angular
**/.idea
cdk.out
**/data/**.csv

.env

# --- Guidance publication hardening ---
# Transient artifacts (node_modules, dist, .DS_Store, .vscode already covered above)
__pycache__/
*.pyc

# Local environment (.env already covered above)
.env.development
.env.production
.env.local

# Triton model artifacts — regenerated by source/triton/export_models.py
# during deploy.sh (Step 2). Not source; do not commit.
source/triton/model_repository/**/*.onnx

# Deployment-state files
.cognito-outputs.json
.frontend-outputs.json
.frontend-vanilla-outputs.json

.kiro/